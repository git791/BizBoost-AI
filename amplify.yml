version: 1
frontend:
  phases:
    preBuild:
      commands:
        - cd bizboost-ui
        - npm install
    build:
      commands:
        - npm run build
  artifacts:
    baseDirectory: bizboost-ui/.next
    files:
      - '**/*'
  cache:
    paths:
      - bizboost-ui/node_modules/**/*